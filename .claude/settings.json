{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "npx tsc --noEmit 2>&1 | tail -20"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'CMD=$(cat /dev/stdin | jq -r .tool_input.command 2>/dev/null); if echo \"$CMD\" | grep -qEi \"rm\\s+-rf|drop\\s+table|drop\\s+schema|truncate|delete\\s+from|supabase\\s+db\\s+reset|git\\s+push.*--force\"; then echo \"BLOCKED: commande dangereuse détectée: $CMD\" >&2; exit 2; fi'"
          }
        ]
      }
    ]
  }
}
